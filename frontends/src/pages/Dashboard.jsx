import React from 'react';
import { Typography } from '@mui/material';

function Dashboard() {
  return (
    <div>
      <Typography variant="h4" gutterBottom>Dashboard</Typography>
      {/* We will add cards and charts here next! */}
    </div>
  );
}

export default Dashboard;
